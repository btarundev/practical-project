# Supabase Experiments

Create a Supabase project, then run `schema.sql` in the SQL Editor. Copy the project URL and anon key into your local environment, never into a committed file. `client.js` demonstrates CRUD, Auth and Storage; `edge-function.ts` demonstrates an authenticated Edge Function.
