# MySQL Experiments

Run `schema.sql` in MySQL Workbench or the MySQL CLI. It covers database/table creation, constraints, CRUD, functions, joins, subqueries, views, indexes, procedures, triggers, transactions, normalization, and advanced SQL. Replace credentials only in your local client; do not commit secrets.
