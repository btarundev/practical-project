# Node.js Backend Experiments

Install with `npm install`, then run `npm start`. The starter demonstrates an HTTP server, Express routing, middleware, JSON responses, validation, errors, and CRUD-style endpoints. Add a database driver such as `mysql2` or `@supabase/supabase-js` for persistence.
